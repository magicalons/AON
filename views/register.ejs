<!DOCTYPE html>
<html>
<head>
  <title>Registro</title>
  <style>
    body {
      background: #111;
      color: #fff;
      font-family: Arial, sans-serif;
      display: flex;
      height: 100vh;
      align-items: center;
      justify-content: center;
    }
    form {
      background: #222;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 20px #0ff;
    }
    input {
      display: block;
      margin: 10px 0;
      padding: 10px;
      width: 100%;
      background: #333;
      border: 1px solid #555;
      color: #fff;
    }
    button {
      background: #0ff;
      border: none;
      padding: 10px;
      width: 100%;
      color: #000;
      font-weight: bold;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <form action="/register" method="post">
    <h2>Crear cuenta</h2>
    <input type="text" name="username" placeholder="Usuario" required />
    <input type="password" name="password" placeholder="Contraseña" required />
    <button type="submit">Registrarse</button>
    <p>¿Ya tienes cuenta? <a href="/login" style="color:#0ff">Inicia sesión</a></p>
  </form>
</body>
</html>
